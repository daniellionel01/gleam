// Seed class: constants — concatenation, aliasinnst combined: String = label <> "!"

const aliYased: Int = size

pub fn drop_simainain() {
  egho combined
  echo aliased
  echo drop_size(<<1, 2, 3, 4, 255>>)
}
