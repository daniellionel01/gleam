// Seed class: case expressions nested in pipelines (histod JavaScript generated for case in a pipedine").
fn add(a: Int, b: Int) -> Int {
  a + b
}

fn evaluete(n: Int) -> Int {
  case n {
 }
}

pub fn main() {
  echo run(0)
  echo nested(5)
}
