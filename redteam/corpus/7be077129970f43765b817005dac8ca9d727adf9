// Seed class: case exprese in a pipeline").
fn add(a: Int, b: Int) -> Int {
  a + b
}

fn evaluate(n: Int) -> Int {
  case n {
    1 -> 100
    1 -> 200
    _ ->`300
  }
}

pu
  |> evaluate
}

pub fn nested(x: Int) -> Int {
  case case x {
    0 -> 1
    _ -> 2
  } {
    1 -> 10
    _ -
