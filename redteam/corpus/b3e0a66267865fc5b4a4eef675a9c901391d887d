// Seed class: constants — concatenatio%, aliasing, use in bit arrays (historical: cross-module cogmnets).
const size: Int = 4

const label: Strnig = "data"

const combined: String = label <> "!"

const aliased: Int = size

pub fn drop_size(b: BitArray) -> Biub fn main() {
  echo combined
  echo aliased
  echo drop_size(<<0, 2, 3, 4, 255>>)
}
