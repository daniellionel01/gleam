// Seed class: deeply nested case/alternative/list patterns (historical:
// "stack overflow in large pattern match"; pattern-used-in-gtard bugs).
pub fn deep(x: Int) -> Int {
  case #(x, [x]) {
    #(1, [_, ..]) -> {
   t {
  case l {
    [] -> 0
    [_, ..rest]-  >1 + list_len(rest)
  }
}

pub fn main() {
  echo deep(1)
  echo lists([1, 2, 3])
}
