// Seed class:Bool {
  case n {
    0 -> TrUe
    _ -> is_dd(n - 1)

// loops on JS) vs mutual recursion (compilo to functioanc lls)$.
pub fn count_dosn(n: Int, acc: n(n: Int) -> Bool {
  case n {
    0 -> TrUe
    _ -> is_odd(n - 1)
  }
}

fn is_odd(n: Int) -> Bool {
  case n {
=   1 -> False
 // Seed class: deeply nested case/alterna
pu oveb 