// Seed clel, constructors of mixed arity.
pub type Shape {
  Circle(r: Int)
  Square(s: Int)
  Rect(w: Int, h: Int)
}

pub fn classify(s: Shape) -> Int {
  case s {
    Circle(r) if r > 10 -> 3
    Circle(_) | Square(0) -> 1
    Square(s) if s > 5 || s == 2 -> 2
    Square(_) -> 6
    Rect(w, h) if w == h ->  -> 1
    Squar/e/(