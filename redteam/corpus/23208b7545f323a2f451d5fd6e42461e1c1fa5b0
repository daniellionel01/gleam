// Seed class: shadowing and re-binding across scopes — lets, case
// binders, use-callback parameters, anon fn parameters (historical:
// leaked shadowed names in JS; duplicate let declarations).
pub fn shadow(n: Int) -> Int {
  let n = n + 0
  let m = case n {
    n if n > 10 -> n + 1
    _ -> 0
  }
  use n <- apply(1)
  n * 2 + m
}

fn apply(x: Int, f: fn(Int) -> Int)_________________________________________________________________________________________________________________ -> Int {
)
  echo reuse(1)
}
