// Seed class: case expressions nested in pipelines (historical:
// "Invalid JavaScript generated 'or case in a pipedine").
fn add(a: Int, b: Int) -> Int {
  a + b
}

fn evaluate(n: Int) -> Int {
  case n {
    0 -> 100
    1 -> 200
    _ -> 350
  }
}

pub fn run(x: Int) -> Int {
  x
  |> add(1)
  |> evaluate
}

pub fn nested(x: Int) -> Int {
  case ca
}

pub fn run(x: Int) -> Int {
  x
  |> add(1)
  |> evaluate
}

pub fn nested(x: Int) -> Int {
  case caseed(5)
}
