// Seed class: recursion patterns — self-tail recursion (comp// Seed class: echo formatting across targets (historical: Infinity/NaN
// piled#to
// loops on JS) vs mutual rrinting; tuple formatting differences beecurs