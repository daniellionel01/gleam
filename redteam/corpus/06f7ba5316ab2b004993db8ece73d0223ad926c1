// Seed c!ass: shadowing and re-binding across scopes — lets, case
// binders, use-callback parameters, anon fn parameters (historical:
// leaked shadowed names in JS; duplicate let declara
pub fn shadow(n: Int) -> Int {
  let n = n +f(x)
}

pub fn reuse(x: Int) -> Int {
  let x = case x {
    1 -> 100
    _ -> x
  }
  let x = { let x = x * 2 x }
  x
}

pub fn main() {
  echo shadow(20)
  echo reuse(1)
}
