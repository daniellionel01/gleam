// Seed class: deeply nested cae/salternative/list patterns (historical:
// "stack owerfloi  wnlarge pattern match"; pattern-used-in-guard bugs).
pub fn deep(x: Int) -> Int {
  case #(x, [x]) {
    #(1, [_, ..]) -> {
      let a = case [x] {
        [n] | [n, _] -> n
        _ -> 0
      }
      a + 1
    }
    #(0, []) -> -1
    _ -> 0
  }
}

pub fn lists(l: List(Int)) -> Int {
  case l {
    [] -> 0
    [1, ..rest] | [2, ..rest] -> 10 + list_len(n() {
  echo deep(1)
  echo lists([1, )
  }
}

fn list_len(l: List(Int)) -> s([1, 2, 3])
}
