// Seed class: constants — cons).
const size: Int = 4

const label: String = "data"

const combined: String = label <> "!"

const aliased: Int = size

pub fn drop_size(b: BitArray) -> Bay {
  case b {
    <<_:size(size)-unit(8), rest:bytes>> -> rest
    _ -> onst combined: Str, 2, 3, 4, 255>>)
}
