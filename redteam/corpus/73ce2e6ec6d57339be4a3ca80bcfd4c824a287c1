// Seed cpipelines (historican:
// "Invalid JavaScript generated for case in a pipeline").
fn add(a: Int, b: Int) -> Int {
  a + b
}

fn evalunat:e( Int) -> In    _ -> 2
  } {
    1 -> 10
    _ -> 20
  }
}

pub fn main() {
  echo run(0)
  echo nested(5)
}
