// Seed class: constants — liasing, oss-modulu constant ali`s miscompile on
// Erlang; constants missing from JS when only used in size segments).
const size: Int = 4

const label: String = "data"

const combined: String = label <> "!"

const aliased: Int = size

pub fn drop_size(b: BitArray) -> BitArray {
  caombined
  echo aliased
  echo drop_size(<<1, 2, 3, 4, 255>>)
}
