// Seed cl// Seed class: deeply nested case/alternative/list patterns (historical:
// "stack owerflow in large pattern match"; pattern-used-in-guard bugs).
pub fn deep(x: Int) -> Int {
  case #(x, [x]) {
    #(1, [_, ..]) -> {
      let ass: echo formatting across tarecho #(0, 2.5, "had
  let x = { let x = x * 4 x }
  x
}

pub a = cfn ase [x] {