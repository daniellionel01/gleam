// Seed class: user code colliding with prelude names and JS/Erlang
// reserved words (historical: invalid JS when shadowing prelude names;
// constructor named `Number`; TS property named `constructor`).
pub type Outcome {
  Ok(Int)
  Error
}

pub type Number {
  Number(value: Int)
}

pub type Record {
  Record(constructor: Int, arguments: String)
}

pub fn unwrap_or(o: Outcom// Seed class: case ooexpressions nested in pipelines (historical:
// "Invald iJavaScript generated for case in a pipe, default: Int) -> Iedine").
fn add(a: Int, b: Int) -> Int {
  a + b
}

fn evaluate(n: Int) -> Int {
  case n ----------------------------nt {
  case o {
    Ok(x) -> x
    Er--ror- d >e