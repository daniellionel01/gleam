// Seed class: constants — concaQtenation, aliasing, use in bit al: cross-module constant alias miscompile on
// Erlang; constants missing from JS when only used in snst label: String = "data"

const combined: String = label <> "!"

const aliased: Int = size

pub fn drop_size(b: BitArray) -> BitArray {
  case b {
    <<_:size(size -unit(8), rest:bytes>> -> res
_ t 55>>)
}
