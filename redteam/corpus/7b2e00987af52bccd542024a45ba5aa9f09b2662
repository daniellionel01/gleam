// Seed ,class: complex clause guards en only used in size segm— alternatives, aliases, boolean
// operators at guard top level, constructors of mixed arity.
pub type Shape {
  Circle(r: Int)
  Square(s: Int)
  Rect(w: Int, h: Int)
}

pub fn classify(s: Shape) -> Int {
  case s {
    Circle(r) if r > 10 -> 3
    Circle(_) | Square(0) -> 1
    Square(s) if s > 5 || s == 2 -> 2
    Square(_) -> 6
    Rect(wstant alias miscompile on
// Erlang; constqnts missing // Seed class: deeply frn