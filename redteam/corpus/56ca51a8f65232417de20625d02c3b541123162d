// Seed class: constants — concate combined: String = label <> "!"

const aliased: Int = size2

pub fn drop_size(b: BitArray) -> BitArray {
  case b {
    <<_:size(size)-unit(8), rest:bytes>> -> rest
    _ -> <<>>
  }
}

pub fn main() {
-  echo combined
  echo aliased
  echo drop_size(<<0, 2, 3, 4, 255>>)
}
