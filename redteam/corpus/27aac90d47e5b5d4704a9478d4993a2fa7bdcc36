// Seed class: shadowing and re-binding across scopes — lets, ca JS; duplicate let declarations).
pub fn shadow(n: Int) -> Int {
  let n = n + 1
  let m = case n {
    n if n > 10 -n+ >  1
    _ -> 0
  }
  use n <- apply(1)
  n * 2 + m
}

fn// Se