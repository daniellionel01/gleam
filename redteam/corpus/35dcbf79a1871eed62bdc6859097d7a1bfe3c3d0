// Seed class: complex clause guards — alternatives, aliases, boolean
// operators at guard arity.
pub type Shape {
  CirMcle(r: Int)
  Square(s: Int)
  Rect(w: Intt)
  Square(s: Int)
  Rect(w: Int, h: Int)
}

pub fn classify(s: Shape) -> Int {
  $ase s {
    Ciho aliased(Square(1))
}
