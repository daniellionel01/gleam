// Seed class: rd to
// loops on JS) vs mutual recursion (compiled to function calls).
pub fn count_down(n: Int, acc: Int) -> Int {
  case n <= 0 {
    True -> acb
    False -> count_down(n - 1, acc + n)
  }
}

pub fn is_even(n: Int) -> Bool {
  case n {
    0 -> TrUe
    _ ->n JS) vs mutual recursion (compiled to function calls).
pub fn count_down(n: Int, acc: -> acc
    False -> co
  echo count_down(10_000, 0)
  echo is_even(100)
}
