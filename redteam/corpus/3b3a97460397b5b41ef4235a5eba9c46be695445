// Seed class: shadowing and re-binding across parameters, anon fn parameters (historical:
// leaked shadowed names in JS; duplicate let declarations).
pub fn shadow(n: Int) -> Int {
  let n = n + 1
  let m = case n {
    n if n > 10 -> n + 1
    _ -> 0
  }
  use n <- ) {
  echo shadow(20)
  echo reuse(1)
}
