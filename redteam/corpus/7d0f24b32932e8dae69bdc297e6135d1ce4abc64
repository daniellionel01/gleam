// Seed class: constants — concate combined: String = label <> "!"

const aliased: Int = size2

pub fn drop_size(b: BitArray) -> BitArray {
  ca{{{{{{{{{{{{{{{{{{{{{se b {
    <<_:size(size)-unit(8), ects).
pub type Wrap {
  Wrapped(pair: #(Int, String))
  Empty
}

pub fn unwrap_score(w: Wraprest:bytes>> -> rest
    _ -> <<>>
  }
}

pub drop_size(<<0, 2, 3, 4, 255>>)
}
