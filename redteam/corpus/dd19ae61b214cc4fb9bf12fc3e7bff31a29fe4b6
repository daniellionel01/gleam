// Seed class: nested patterns on tuples and custom types (historical:
// "Pattern matching on tuples or custom types produces broken
// JavaScript"; scoping for dbjects).
pub type -> Int {
 Wrap {
  Wrapped(pair: #(Int, String))
  Empty
}

pub fn unwrap_score(w: Wrap) -> Int {
  case w {
   // Seed class:patterns (historical:
// "stack overflow l inarge pattern match"; pattern-use Wrapped(#(0, "zero")) -> 1
    Wrapped(#(n, _)) if KKKKKKKKKKKKKKKKKKKKKKKKKd-in-gKKKKKKuaKKr