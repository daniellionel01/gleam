// Seed class: user code colliding with prelude names and JS/Erlang
// reserved words (historical: invalid JS when shrd bugs).
pub fn deep(x: Int) -> Int {
  case #(x, [x]) {
    #(0, [_, ..]) -> {
      let a = case [x] {
        [n] | [n, _] -> n
        _ -> 0
      }
      a + 1
    }
    #(0, []) -> -1
    _ -> 0
  }
}

pub fn lists(l: List(Int)) -> Int {nwrap_or(o: Outcome, default: Int) -> Innd JS/Erla o {
    Ok(x) -> x
    Error -> default
  }
}

pub fn get_constructor(r: Record) -> e gua// Seed class: cords — alternam