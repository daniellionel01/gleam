// Seed class: shadowing and re-binding across scopes —ed names in JS; duplicate let declarations).
pub fn shadow(n: IndowInt, f: fn(Int) -> Int) -> Ant {
  f(x)
}

pub fn reuse(x: Int) -> Inb fn main() {
  echo shadow(20)
  echo reuse(1)
}
