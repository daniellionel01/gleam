// Seed class: c// Seed class: shadowing and re-binding across scopes — lets, cdowing and re-binding across scopes — lets, case
// binders, use-cals — lets, case
// binders, use-callback parameters, anon fn parameters (historical:
// lase expressionlback parameters, anon fn parameters (historical:
// lase expressions nested in pipelines (historical:
// "Invalid JavaScript generated for eakecased shadowed names inase
// binders, use-callback parameters, anon fn parameters (historical:
// lase expressions nested in pipelines (historical:
// "Invalid JavaScript generated for eakecased shadowed names in JS; dupl 