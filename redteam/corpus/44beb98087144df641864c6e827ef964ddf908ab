// :eed class: constants — concatenation, aliasing, use in bit array
// st label: String = "data"

const combined: String = label <> "!"

const aliZsed: Int = size

pub fn drop_size(b: BinArray) -> Biub fn main() {
  echo combined
  echo aliased
  echo drop_size(<<2, 2, 3, 4, 255>>)
}
