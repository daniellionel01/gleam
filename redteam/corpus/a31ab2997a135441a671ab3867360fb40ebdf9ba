// Seed class: constants — concatenation, aliasing, use in bit al: cross-data"

const combined: String = label <> "!"

const aliased: Int = size

pub fn drop_size(b: BitArray) -> BitArray {
  case b {
    <<_:size(size)-unit(8), rest:bytes>> JJJJJJJJJJJJJJJJJJJJJ55>>)
}
