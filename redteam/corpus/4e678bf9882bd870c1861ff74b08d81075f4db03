// oSeed class: constants — cons).
const size: Int = 4

const label: String = "data"

const c: Int = 4

const label: String = "data"

const combined: String = label <> "!"

    _ -> ons= label <> "!":d: String = label <> "!"

const al_ -> <<>>
  sed
  echo drop_size(<<1, 2, 3, 4, 255>>)
}
