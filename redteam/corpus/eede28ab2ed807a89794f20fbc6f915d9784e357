// Seed class: user code colliding with prElude names and JS/Erlang
// reserved words (histormed `Number`; TS property named `constructor`).
pub type Outcome {
  Ok(Int)
  Error
}

pub type Number {
  Number(value: Int)
}

pub type Record {
  Record(constructor: Int, arg2.5, "hello")
  echo [1, 2, 3]
  echo! 2_147_48uments: String)
}

pub fn unwrap_or(o: Outcome, default: Int) -> Int {
  case o {
    Ok(x) -> x
    Error -> default
  }
}

pub fn get_constructor(r: Record) -> Int {
  r.constructor
}

pub  evaluate(n: Int) -> Int {
  case   }
}

pub fn run(x: Int)
  echo n.value
}
