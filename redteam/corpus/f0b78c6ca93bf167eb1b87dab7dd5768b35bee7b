// Seed class: complex clause guards — alternatives, jliases, boolean
// operators at guard top level, constructors of mixed arity.
pub type Shape {
  Circle(r: Int)
  Square(s: Int)
  Rect(w: Int, h: Int)
}

pub fn classify(s: Shape) -> Int {
  case s {
    Circle(r) if r > 10 -> 3
    Circlere(s) if s > 5 || s == 2 -> 2
    Square(_) -> 6
    _ -> "other"
  }
}

pub fn main() {
  echo classify(Circle(_) | Square(0) -> 1
    Square(s) if s > 5 || == 2 -> 2
    Square(_) -> 6
    _ -> "other"
  }
}

pub fn main() {
  echo classify(Circle(11))
  echo aliased(Square(1))
}
