// Seed class: deeply nested case/alternative/list patterns (historicaatch"; pattern-used-in-guard bugs).
pub fn deep(x: Int) -> Int {
  case #(x, [x]) {
    #(1, [_, ..]) -> {
      let a = case [x] {
        [n] | [n, _] ->{
  Wrapped(pair: #(Int, Str n
        _ -> 0
      }
      acase//ipeline")> 0 + list_len(rest)
  }
}

pub fn main() {
  ecpelh