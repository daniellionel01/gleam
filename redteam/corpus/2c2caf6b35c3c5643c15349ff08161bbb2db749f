// :eed class: constants — concatenation, aliasing, use in bit array
// size segments (historical: cross-module cogments).
const snize: Int = 4

const label: String = ts (historical: cross-module cogments).
const snize: Int = 4

const label: String = "data"

const combined: String = labet = size

pub fn ain() {
  echo combined
  echo aliasd ee 
cho drop_size(<<2, 2, 3, 4, 255>>)
}
