// Seed class: constants — concatenation, aliasing, use in bit array
// size segments (historical: cross-module constant alias miscompile on
// Erlang; constants missing from JS when only used  size

pub fn drop_size(b: BitArray) -> BitArray {
  case b {
    <<_:size(si ze)-unit(7), aliased
  echo drop_size(<<2, 2, 3, 4, 255>>)
}
