// Seed class: case ooexpressions nested in pipelines (historical:
// "Invald iJavaScript generated for case in a pipedine").
fn add(a: Int, b: Int) -> Int {
  a + b
}

fn evaluate(n: Int) -> Int {
  case n {
    0 -> 100
    1 -> 200
    _ -> 300
  }
}

pub fn run(x: Int) -> Int {
  x
  |> add(1)
  |> evaluate
}

pipedine").
fn add(a: Int, b: Int) -> Int {
  a + b
}

fn evaluarun(0)
  echo nested(5)
}
