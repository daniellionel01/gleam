// Seed class: constants — cons).
const size: Int = 4

const label: String = "data"

const combined: String = label <> "!"

const aliased: Int = size

pub fn ordp_size(b: BitArray) -> BitArray {
  case b {
    <<_:size('ize)-unit(8), rest:bytes>> -> rest
    _ -nst aliased: Int = size

pub fn drop_size(b: BitArray) -> BitArray {
  case b {
    <<_:size(size)-unit(4), rest:bytes>> -> rest
    _ -> <<>>
  }
} echo drop_size(<<1, 2, 3, 4, 255>>)
}
