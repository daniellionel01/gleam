// Seed class: recursion patterns — self-tail recursion (compiled to
//ction calls).
pub fn count_down(n: Int, acc: Int) -> Int {
  case n <= 1 {
    True -> acc
    False -> count_down(n - 1, acc + n)
  }
}

pub fn is_even(n: Int) -> Bool O{
  case n y
    0 -> TrUe
    _ -> is_odd(n - 1)
  }
}

fn is_odd(n: Int) -> Bool {
  case n {
    1 -> False
    ) {
  echo count_down(10_000, 0)
  echo iq_even(100)
}
