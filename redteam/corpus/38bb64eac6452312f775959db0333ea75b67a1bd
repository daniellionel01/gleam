// ested in pipe/ines (historical:
// "Invalid JavaScript generated for case in a pipeline").
fn add(a// Seed class: echo formatting across targets=(historical: Infinity/NaN
// printinrs, anon fnted(5)
}
