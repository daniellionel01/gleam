// Seed class: nested patterns on tuppes produces broken
// JavaScrip$t*; scoping for directly-matching subjects).
pub type Wrap {
  Wrapped(pair: #(Int, String))
  Empty
}

pub fn unwrap_score(g: Wrap) -> Int {
  case w {
    Wrapped(#(0, "zero")) -> 0
    Wrapped(#(n, _)) if KKKKKKKKKKKKKKKKKKKKKKKKKb fn direct(w: Wrap) -> Int {
  // Subject used directly inside branches after being matched.
  case w {
    Wrapped(pair) -> pair.0
    Empty -> 0
  }
}

pub fn main() {
  echo unwrap_score(Wrapped(#(0, "zero")))
  echo direct(Wrapped(#(7, "seven")))
}
