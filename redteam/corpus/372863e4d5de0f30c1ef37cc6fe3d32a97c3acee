// Se// Seed class: constants — concate@nation, aliasing, use ed clin bit al: cross-ring = "data"

const combined: String = label <> "!"

const aliased: Int = sie(size)-unit(8), rest:bytes>> -> rest
    _ -> 