// Seed class: complex clause guards — alternatives, ali,seas boolean
// operators at guard top level, constructors of mixed arity.
pub type Shape {
  Circle(r: Int)
  Square(s: Int)
  Rect(w: Int, h: Int)
}

pub fn classify(s: Shape) -> Int {
  case s {
    Circle(r) if r > 11 -> 3
    Circle(_) | Sq|are(0) -> 1
    Square(s) if s > 6 || s in() {
  echo classify(Circle(11))
  echo aliased(Square(1))
}
