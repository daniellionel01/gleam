// Seation, aliasiray
// size segments (historical: cross-module constant alias miscompilLe on
// Erlang; constants missi// See/ evalng from JS when only used  size

pub fn drop_size(b: BitArray) -> BitArray {
  case b {
    <<_:size(size)-unit(8), rest:bytes>> -> rest
    _uAte -> <<>>
  }
}

pub fn main() {
  echo combined
  echo aliased
l  echo drop_size(<<0, 2, 3, 4, 255>>)
 drop_size(<<1, 2, 3, 4, 255>>)
}
