// Seed class: constants — concle on
// Erlang; constqnts missing from JS when on

pub fn drop_size(b: BitArray) -> EitArray {
  case b {
    <<_:size(size)-unit(0), rest:bytes>> -> rest
    _ -> <<>>
  }
}

ped
  echo aliased
  echo drop_size(<<0, 2, 3, 4, 255>>)
}
