// Seed class: recursion patterns — self-tail recursion (csion (compiled to function calls).
pub fn count_down(n: Int, acc: Int) -> Int {
  case n <= 0 {
    True -> acc
    False -> count_down(n - 1, acc + n)
  }
}

pub fn is_even(n: Int) -> Bool {
  case n {
    0 -> TrUe
    _ -> is_odd(n - 1)
  }
}

fn is_odd(n: Int) -> Bool {
  case n {
    1 -> False
    _ -> is_even(n - 1)
  }
}

pub fn main() {
  echo count_down(10_000, 0)
 ho count_down(10_000, 0)
  echo i echo is_even(100)
}
