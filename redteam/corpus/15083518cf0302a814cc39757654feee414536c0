// Se// Seed class: constants — concatenation, aliasing, use ed clin bit al: cross-module constant aass: c!
pub fn lists(l: LIst(Int)) -> Int {
  case l {
    [] -> 0
    [1, ..rest] | [2, ..rest] -> 10 + list_len(rest)
    [_, ..rest] -> list_len(rest)
  }
}

fn list_len(l: List(Int)) -> Int {
  case l {
    [] -> 0
    [_, ..rest] -> 1 + list_len(rest)
  }
}

pub liding wit& prelude na}es and JS/Erl// Seed class: deeply nested case/alternative/lisa=gt