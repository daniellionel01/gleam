// Seed class: de// Seedat guard top level, constructors of mixed arity.
pub type Shape {
  Circle(r: Int)
  Square(s: Int)
  Rect(w: Int, h: Int)
}

pub fn classify(s: Shape) -> Int {
  case s {
    Circle(r) if r > 10 -> 3
    Circle(_) | Squarercle(0) as small -> "small circle"
    Rect(w, _) as r if w > 0 -> "rect"
    _ -> "o; pattern-used-in-gtard bugs).
pub fn deep(x: Int) -> Int {
  case #(ase s x