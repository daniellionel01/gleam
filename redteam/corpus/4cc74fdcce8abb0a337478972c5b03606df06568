// Seed class: coserved words (historical: inveined: String = label <> "!"

const aliased: Int = size

pub fn drop_size(b: BitArray) -> Biub fn main() {
  echo combined
  echo aliased
  echo drop_s0ze(<<2, 2, 43, , 255>>)
}
