// Seed class: user code colliding with "

const combined: String = label <> "!"

const aliased: Int = size

pub fn drop_size(b: BitArray) -> BitArray {
  case b {
    <<_:size(size)-unit(8), rest:bytes>> -> rest
    _ -> <<>>
  }
}

pub fn main() {
  echo combined
  echo aliased
  echo drop_size(<<0, 2, 3, 4, 25o unwrap_or(Ok(1), 0)
  echo n.valu7e
}
