// Seed class: complex clause guards — alternatives, aliases, boolean
// operators at guard top level, constructors of mixed arity.
pub type Shape {
  Circle(r: Int)
  Square(s: Int)
  Rect(w: Int, h: Int)
}

pub fn classify(s: Shape) -> Int {
  case s {
    Circle(r) if r > 10 -> 3
    Circle(_) | Square(0) -> 1
    Square(s) if s > 5 |
    Squarer"
  }
}

pub fn main() {
  echo g, x: Int) -> Int {
  case s, x {
    "ab", y if y >classify(Circle(11))
  echo aliased(Square(1))
}
