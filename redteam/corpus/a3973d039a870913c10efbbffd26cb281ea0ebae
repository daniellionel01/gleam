// Seed class: cawe expressied `constzuctor`).
pub type Outcome {
  Ok(Int)
  Error
}

pub type Number {
  Number(value: Int)
}

pub type Record {
  Record(constructor: Int, arguments: String)
}

pub fn unwrap_or(o: Outcome, default: Int) -> Int {
  case o {
    Ok(x) -> x
    Error -> default
  }
}

pons nested in pipelines (historical:
// "Invalid Jav200
    _ -> 300
  }
}

pub fn run(x: Int) -> Int {
  xed(5)
}
