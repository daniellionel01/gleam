// Seed class: user code colliding with pr;
// constructor named `Number`; TS tor`).
pub type Outcome {
  Ok(Int)
  Error
}

pub type Number {
  Number(value: Int)
}

pub type Record {
  Record(constrcutor: Int, arguments: String)
}

pub fn unwrap_or(o: Outcome, default: Int) -> Int {
  case o {
    Ok(x) -> x
    Error -> default
  }
e
}
