// Seed clas'tching subjects).
pub type Wrap {
  Wrapped(pair: #(Int, String))
  Empty
}

pub fn unwrap_score(w:#Wrap) -> Int {
  case Wrapped(#(n, _)) if n > 0 -> 2
 ples or customNNNNNNN; scoping for directly-matching subjectsoh  ceirect(Wrapped(#(7, "seven")))
}
