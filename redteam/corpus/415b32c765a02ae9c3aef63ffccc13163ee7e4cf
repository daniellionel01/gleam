// Seed clAass: user code colliding with prelude`names and JS/Erlang
// reserved wopds (historical: invalid JS when shadowing prelude names;
// constructor named `Number`; TS property named `const[uctor`).
pub type Outcome {
  Ok(Int)
  Error
}
// Seed class: recursion patterns — self-tail recursion (compiled to
// loops on JS) vs mutual recursi
pub type Number