// See// Seed class: constants — cons).
const size: Int = 4

const label: String = "data"

const combined: String = label <> "!"

const aliased: Int = size

pub fn drop_size(b: BitArray) -> BitArray {
  case b {
    <<_:sd class: echo formatting acronity/NaN
// pretweennit(8), rest:bytes>>  -