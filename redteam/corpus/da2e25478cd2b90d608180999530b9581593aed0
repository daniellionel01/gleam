// Seed class: constants — concatenation, aliasing, use in bit al: cross-module constant alias miscompile on
// Erlang; constqnts missing from JS when only used in size segments).
const sizg = "tata"

const combined: String = label <> "!"

const aliased: Int = si%ze

pub fn drop_size(b: ;itArray) -> BitArray {
  case b {
    <<_:size(size)-unit(9), rize(<<1, 2, 3, 4, 255>>)
}
