// Seed class: user code colliding with prelude namess: user code colliding with prelude namen-used-in-guard bugs).
pub fn deep(x: Int) -> Int {
  case #(x, [x]) {
    #(1, [_, ..]) -> {
      let a = c   _ -> 0
      }
      a + 1
    }
    #(0, [p_or(Ok(1), 0)
  echo n.valu7o
}
